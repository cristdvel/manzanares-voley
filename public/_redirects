# Cloudflare Pages solo evalua rutas en _redirects, no el hostname, asi que
# el redirect www -> dominio raiz se hace con una "Redirect Rule" en el panel
# de Cloudflare (ver DEPLOY.md). Este archivo queda para futuras reglas de ruta.
